// Hier sind die fehlenden Java-Dateien für das Backend (Spring Boot).

// src/main/java/com/example/excelexport/ExcelExportApplication.java
package com.example.excelexport;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ExcelExportApplication {
    public static void main(String[] args) {
        SpringApplication.run(ExcelExportApplication.class, args);
    }
}

